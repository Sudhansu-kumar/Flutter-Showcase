import 'package:flutter/material.dart';

class OrderMedicine extends StatelessWidget {
  const OrderMedicine({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Order Medicine\'s Online'),
      ),
    );
  }
}